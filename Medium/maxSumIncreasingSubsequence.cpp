#include <vector>
#include <map>
#include <algorithm>
using namespace std;

vector<vector<int>> maxSumIncreasingSubsequence(vector<int> array) {
    vector<int> sortList = array;
    sort(sortList.begin(), sortList.end());
    map<int, int> findPrev;
    for (int i = 0; i < array.size(); ++i){
        if ()
    }
    return {
        {0}, // Sum of sequence.
        {},  // Elements of the sequence.
    };
}
